{
  "name": "@soc-watch/web",
  "version": "0.12.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build --configLoader runner",
    "dev": "vite --configLoader runner --host 127.0.0.1",
    "lint": "tsc -b --noEmit",
    "typecheck": "tsc -b --noEmit",
    "test": "node scripts/run-tests.mjs"
  },
  "dependencies": {
    "@soc-watch/ioc": "0.1.0",
    "@soc-watch/protocol": "0.1.0",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
